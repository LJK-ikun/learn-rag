# 虚拟环境（体积大、机器相关，绝不入库）
.venv/

# 真实 API key 只放在 .env，不入库；模板走 .env.example
.env

# 索引落盘产物（M5 切到 Chroma 之后会生成）
index/

# Python 垃圾文件
__pycache__/
*.py[cod]
.pytest_cache/

# 编辑器插件产生的会话日志，不是项目内容
.deep-copilot/

# 调试日志
*.log
debug.log
